<%= render 'header' %>

<div class="container">
  <div class="movie-content">
    <div class="row">
      <div class="col-lg-6">
        <%# <%= cl_image_tag @list.photo.key, height: 200, width: 400, crop: :fill, class: 'img-border' %>
      </div>
      <div class="col-lg-6">
        <h2><%= @list.name %> Movies</h2>
        <div class="btn-style">
          <p><%= link_to "Bookmark",  new_list_bookmark_path(@list), class: 'btn-book' %></p>

          <%# <p><%= link_to "Delete", edit_list_path(@list), class: 'btn-delete' %>
        </div>
        <p></p>
      </div>
    </div>
